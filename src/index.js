import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import ImageHost from './ImageHost/ImageHost';

ReactDOM.render(
  <React.StrictMode>
    <ImageHost></ImageHost>
  </React.StrictMode>,
  document.getElementById('root')
);

